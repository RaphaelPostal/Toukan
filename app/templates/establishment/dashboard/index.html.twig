{% extends 'base.html.twig' %}
{% block body %}
    {% if not subscriptionActive %}
        <p class="bg-red-200 text-red-600 p-4">Vous n'avez pas encore d'abonnement, vos fonctionnalités sont restreintes.
            <a class="underline" href="{{ path('app_subscription_pricing') }}">Voir les abonnements</a>
        </p>
    {% endif %}
    <h1 class="text-center font-bold text-2xl">Dashboard</h1>
    <p>Bah c'est un dashboard mais y'a rien encore zebi</p>

    <a class="underline" href="{{ path('establishment_profile') }}">Accéder à mon profil</a>
{%endblock%}