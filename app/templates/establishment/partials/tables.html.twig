    <table class="table">
        <thead>
        <tr>
            <th>{{ 'table_number'|trans({}, 'table_management') }}</th>
            <th>{{ 'edit'|trans({}, 'table_management') }}</th>
            <th>Qr Code</th>
            <th>{{ 'delete'|trans({}, 'table_management') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for table in tables %}
            <tr>
                <td>
                    <input type="checkbox" data-table-name="{{ table.number }}" data-table-id="{{ table.id }}"
                           name="qrCodeToPrint">

                    {{ table.number }}
                </td>
                <td>
                    <button type="button" onclick="set_frame_src({{ table.id }}, 'table_edit')"
                            class="transition duration-150 ease-in-out admin-button-edit" data-bs-toggle="modal"
                            data-bs-target="#modal-form-edit-table">
                        <i class="fa-solid fa-pen"></i> {{ 'edit'|trans({}, 'table_management') }}
                    </button>
                </td>
                <td>
                    <button class="btn admin-button-edit qr_code_print_one" data-table-name="{{ table.number }}"
                            data-table-id="{{ table.id }}"><i
                                class="fa-solid fa-print"></i>{{ 'print_qrcode'|trans({}, 'table_management') }}
                    </button>
                </td>
                <td>
                    <form method="post" action="{{ path('app_table_delete', {'id': table.id}) }}"
                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette table ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table.id) }}">
                        <button class="btn admin-button-delete"><i
                                    class="fa-solid fa-trash"></i> {{ 'delete'|trans({}, 'table_management') }}</button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{ 'no_table'|trans({}, 'table_management') }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <button id="qr_code_print_selection" class="btn admin-button-edit no-print" type="submit"><i
                class="fa-solid fa-print"></i> {{ 'print_selection'|trans({}, 'table_management') }}</button>
