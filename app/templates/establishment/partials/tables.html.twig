<table class="table">
    <thead>
    <tr>
        <th>Nom/N° Table</th>
        <th>Modifier</th>
        <th>Qr Code</th>
        <th>Supprimer</th>
    </tr>
    </thead>
    <tbody>
    {% for table in tables %}
        <tr>
            <td>{{ table.number }}</td>
            <td>
                <a href="{{ path('app_table_edit', {'id': table.id}) }}">edit</a>
            </td>
            <td>
                <a href="">Imprimer QrCode</a>
            </td>
            <td>
                <a href="{{ path('app_table_delete', {'id' : table.id}) }}"><i class="fa-solid fa-trash fa-fw"></i> Supprimer</a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="3">Aucune table</td>
        </tr>
    {% endfor %}
    </tbody>
</table>