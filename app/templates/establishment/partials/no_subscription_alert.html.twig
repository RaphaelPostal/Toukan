<div class="bg-red-200 text-red-600 p-4 flex flex-row justify-between">
    <p>
        <i class="fa-solid fa-circle-exclamation"></i>
        {{ 'subscription.no_active_subscription'|trans({}, 'alert') }}
    </p>
    <a class="underline" href="{{ path('app_subscription_pricing') }}">{{ 'subscription.go_to_subscriptions'|trans({}, 'alert') }}</a>
</div>